# 📌 time-schedular

Простой сервис‑планировщик задач на Python, упакованный в Docker.

Этот README предназначен **для людей без опыта Docker и деплоя** — все шаги расписаны максимально подробно.

---

## 📋 Что это за проект

**time-schedular** — это Python‑сервис, который запускается в Docker‑контейнере и выполняет задачи по расписанию.

Docker используется для того, чтобы:

* не устанавливать Python и зависимости вручную
* гарантировать одинаковую работу на любой машине
* упростить запуск и обновление

---

## 🧠 Что нужно заранее

Перед началом убедитесь, что у вас есть:

* 💻 Компьютер или сервер (Linux / macOS / Windows)
* 🌐 Доступ в интернет
* 👤 Пользователь с правами `sudo`

Из программ потребуется:

* **Git** — для скачивания проекта
* **Docker** — для запуска сервиса
* **Docker Compose** — для удобного управления контейнером

---

## 🔧 Шаг 1. Установка Docker (если он ещё не установлен)

### 🔹 Ubuntu / Debian

Откройте терминал и выполните команды **по очереди**:

```bash
sudo apt update
sudo apt install ca-certificates curl gnupg
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-compose-plugin
```

Проверьте установку:

```bash
docker --version
docker compose version
```

Если команды выводят версии — всё установлено корректно.

---

## 📥 Шаг 2. Скачивание проекта

Перейдите в каталог, где будет храниться проект (пример — `/opt`):

```bash
cd /opt
git clone https://github.com/RomanZorkin/time-schedular.git
cd time-schedular
```

После этого структура проекта должна выглядеть примерно так:

```
time-schedular/
├── Dockerfile
├── docker-compose.yml
├── app/
├── logs/
├── pyproject.toml
├── uv.lock
```

---

## 🗂️ Шаг 3. Подготовка папки для логов

Сервис пишет логи в папку `logs/` на вашей машине.

Создайте её (если она отсутствует):

```bash
mkdir -p logs
chmod 755 logs
```

---

## 🚀 Шаг 4. Запуск сервиса

Находясь **в корне проекта** (`time-schedular`), выполните:

```bash
docker compose up -d --build
```

### Что происходит:

* Docker собирает образ из `Dockerfile`
* Создаёт контейнер
* Запускает сервис в фоне (`-d`)

⏳ Первый запуск может занять несколько минут.

---

## 📊 Шаг 5. Проверка, что всё работает

Посмотреть список запущенных контейнеров:

```bash
docker ps
```

Вы должны увидеть контейнер со статусом **Up**.

---

## 📖 Просмотр логов

### 🔹 В реальном времени

```bash
docker logs -f reliable-scheduler
```

### 🔹 Через файлы

```bash
ls logs
tail -f logs/*.log
```

---

## 🛠️ Остановка и перезапуск

### Остановка сервиса

```bash
docker compose down
```

### Перезапуск (после изменений)

```bash
docker compose down
docker compose up -d --build
```

---

## 📦 Обновление проекта

Чтобы обновиться до последней версии из GitHub:

```bash
git pull
docker compose up -d --build
```

---

## ❗ Возможные проблемы

### Docker не найден

```bash
docker: command not found
```

➡ Убедитесь, что Docker установлен и запущен:

```bash
sudo systemctl start docker
```

---

### Контейнер не стартует

Посмотрите логи:

```bash
docker logs reliable-scheduler
```

---

## 📌 Итог

Теперь вы:

* ✔ Скачали проект
* ✔ Запустили его через Docker
* ✔ Знаете, как смотреть логи и перезапускать сервис

Этот README рассчитан на **первый опыт деплоя**, без скрытых шагов и магии.

Если нужно:

* добавить раздел «как работает планировщик»
* описать конфигурацию задач
* сделать версию README для Windows

— просто скажите.
